services:
  - type: web
    name: motozap-sync
    runtime: node
    plan: free
    buildCommand: "echo 'No build needed'"
    startCommand: "node server/sync.js"
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "20"